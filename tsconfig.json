{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "moduleResolution": "Node",
    "outDir": "./dist",

    // Needed for correct imports/exports
    "esModuleInterop": true,

    // Strict type checking
    "strict": true,

    // Skip checking .d.ts from node_modules for faster builds
    "skipLibCheck": true,

    // Avoid case-sensitivity issues on Windows
    "forceConsistentCasingInFileNames": true,

    // 👇 Custom global type definitions (Express augmentation)
    "typeRoots": [
      "./node_modules/@types",
      "./src/types"
    ]
  },

  // 👇 Scan ONLY source files (includes src/types automatically)
  "include": [
    "src"
  ],

  "exclude": [
    "node_modules",
    "dist"
  ]
}
